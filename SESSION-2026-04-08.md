# Session 2026-04-08: Trib Plugin Production Cleanup & Improvement

## Completed Work

### 1. Skill Trigger Strengthening

All three auto-trigger skills updated with concrete tool call patterns and stronger trigger conditions.

**trib-search/skills/search/SKILL.md**
- Renamed folder from `verify/` to `search/`
- Added "unfamiliar or not in training data — search first, never guess"
- Added `Tool Call Patterns` section with exact MCP tool names and parameters (search, batch, scrape, map, crawl)

**trib-agent/skills/workflow/SKILL.md**
- Added "search, review, compare, analyze" to trigger verbs
- Added `Required Tool Call Patterns` section:
  - One-off task: `Agent({ run_in_background: true, mode: "bypassPermissions" })`
  - Worker/Reviewer: `TeamCreate → Agent({ team_name, mode: "bypassPermissions", run_in_background: true })`
  - MANDATORY parameters table

**trib-memory/skills/recall/SKILL.md**
- Changed to "ALWAYS invoke, no exceptions"
- Added compressed/truncated context and session resume triggers
- Added `Tool Call Patterns` section with exact search_memories call patterns

### 2. MCP Instructions — CRITICAL Lines

| Plugin | File | Change |
|--------|------|--------|
| trib-agent | server.mjs | CRITICAL line added: workflow skill must invoke before any task |
| trib-search | server.mjs | CRITICAL line added: search skill must invoke for research; use trib-search not WebSearch |
| trib-memory | services/memory-service.mjs | CRITICAL line added: recall skill must invoke at session start |

### 3. READMEs — All Rewritten from Scratch

All 4 plugin READMEs rewritten to match actual implementation (not plan documents).

- `trib-memory/README.md` — cycle3 not implemented noted, hints removed, injection paths updated
- `trib-agent/README.md` — new file, covers MCP tools, workflows, agents, commands, providers
- `trib-search/README.md` — new file, covers 11 providers, routing, caching, usage tracking
- `trib-channels/README.md` — new file, covers Discord, scheduling, proactive chat, webhooks, events

### 4. Plan Documents Deleted

All outdated plan/research documents removed:

- `trib-agent/AGENT-MANAGER-PLAN.md`
- `trib-memory/MEMORY-DECAY-PLAN.md`
- `trib-memory/CORE-MEMORY-PLAN.md`
- `trib-memory/SEARCH-REDESIGN-PLAN.md`
- `trib-memory/RAG-INJECTION.md`
- `trib-memory/RAG-TRENDS-2025.md`
- `trib-memory/RETRIEVAL-CLASSIFICATION-PLAN.md`

### 5. Dead Code Removal

**trib-search:**
- `lib/ai-cli-worker.mjs` — deleted (disabled worker)
- `lib/ai-cli-worker-host.mjs` — deleted (disabled worker)
- `commands/config.md` — deleted (orphaned doc)
- `dist/server.bundle.mjs` — deleted (stale bundle)
- `dist/` directory — deleted (empty)
- `commands/` directory — deleted (empty)
- `server.mjs` worker comment — removed

**trib-channels:**
- `dist/server.bundle.mjs` — deleted (stale bundle)
- `dist/` directory — deleted (empty)

**trib-memory:**
- `lib/memory-score-utils.mjs` — MEMORY-DECAY-PLAN.md reference removed
- `lib/memory.mjs` — RETRIEVAL-CLASSIFICATION-PLAN reference removed
- `scripts/TUNING-PLAYBOOK.md` — deleted plan doc link
- `trib-agent/README.md` — AGENT-MANAGER-PLAN.md reference removed

### 6. Hints Chain — Complete Removal

Entire hints/per-turn injection system removed. Recall via search_memories covers the use case with better quality (verified by testing: hints 0/6 accurate, recall 5/6 accurate).

**Deleted files:**
- `trib-memory/lib/memory-context-builder.mjs`

**Code removed:**
- `trib-memory/services/memory-service.mjs` — `/hints` GET and POST endpoints
- `trib-memory/lib/memory.mjs` — `buildInboundMemoryContext` import and method
- `trib-memory/lib/memory-recall-store.mjs` — `bulkVerifyHints` function
- `trib-memory/lib/memory-tuning.mjs` — `hintInjection` config object
- `trib-memory/scripts/inspect-recall.mjs` — `countHints`, `with_hints` code
- `trib-channels/lib/memory-client.mjs` — `getHints()` function
- `trib-channels/server.ts` — `getHints` import
- `trib-channels/CONFIG.md` — `retrieval.hintInjection` section and JSON example
- `trib-memory/scripts/TUNING-PLAYBOOK.md` — `buildInboundMemoryContext()` reference

### 7. Singleton Guard — trib-memory Rewrite

Dual-instance race condition fix. When cache and external_plugins versions both start, they no longer kill each other.

**New behavior:**
1. On startup, check PORT_FILE → HTTP GET /health
2. If healthy server exists → enter proxy mode (stdio MCP only, forwards to HTTP `/api/tool`)
3. If no healthy server → start as primary (existing behavior)

**Changes in `services/memory-service.mjs`:**
- Added `isExistingServerHealthy()` — port file + /health check
- Added `runProxyMode(port)` — minimal stdio MCP that proxies tool calls
- Added `/api/tool` HTTP endpoint — proxy target for tool forwarding
- Unified `MEMORY_INSTRUCTIONS_TEXT` constant (removed duplicate `MEMORY_INSTRUCTIONS`)
- Moved `RUNTIME_DIR`, `PORT_FILE`, `BASE_PORT`, `MAX_PORT` to module scope (before proxy check)

### 8. Version Bumps

| Plugin | Before | After |
|--------|--------|-------|
| trib-agent | 0.0.28 | 0.0.29 |
| trib-search | 0.0.12 | 0.0.14 |
| trib-memory | 0.0.44 | 0.0.46 |
| trib-channels | 0.0.12 | 0.0.13 |

### 9. Reviewer Fix — Post-Review Corrections

Issues found by trib-agent:Reviewer, all fixed:

1. `/api/tool` double JSON parse bug (CRITICAL) — removed `JSON.parse()` wrapper
2. trib-memory README stale hints references — injection paths, key files, future work updated
3. trib-search README stale worker/commands references — cleaned
4. trib-channels CONFIG.md hintInjection section — removed
5. TUNING-PLAYBOOK.md buildInboundMemoryContext reference — removed
6. trib-channels version bump — 0.0.12 → 0.0.13

---

## Remaining Work

### Priority 1: Stability

#### Singleton guard — verify in production
- Test proxy mode works when cache + external both start
- Test primary mode works when no existing server
- Verify `/api/tool` endpoint returns correct format after double-parse fix
- Edge case: what if primary crashes while proxy is connected?

### Priority 2: Config & Model Settings

#### trib-memory cycle model configuration
- Current: cycle1/cycle2 provider hardcoded to `{ connection: 'codex', model: 'gpt-5.4-mini' }`
- Need: config UI (setup.html) section for cycle1/cycle2 model selection
- Reference: trib-agent's preset system in `orchestrator/config.js` (provider + model + effort + fast)
- Config shape to support:
  ```json
  {
    "cycle1": { "provider": { "connection": "codex", "model": "gpt-5.4-mini", "effort": "medium", "fast": true }, "interval": "5m" },
    "cycle2": { "provider": { "connection": "claude", "model": "sonnet", "effort": "medium" } }
  }
  ```
- Bring trib-agent's model listing approach (`list_models` tool) for available model discovery

#### Config popup always-on-top (all plugins)
- Current: Chrome `--app=` mode opens but doesn't stay on top; gets buried behind terminal
- Approach: PowerShell `SetWindowPos` with HWND_TOPMOST after 1-second delay
- Applies to all 4 plugins' `setup/setup-server.mjs`
- Also need: when `/open` is called on already-running server, bring window to front (focus steal)
- Consider: single shared setup module to avoid duplicating the fix in 4 places

### Priority 3: Feature Work

#### Workflow auto-execution tool (trib-agent)
- Add `execute_workflow(name, context)` MCP tool
- Automates step chaining: load workflow → execute steps in order → pass results between steps
- Route by model prefix: `native/*` → Agent, `external/*` → delegate
- Current: Lead must manually orchestrate each step

#### Event rule setup command (trib-channels)
- Add `/events` slash command for webhook/watcher/file rule CRUD
- Current: all event rules require manual config.json editing
- Need: interactive setup flow — add rule, test payload, preview template output

### Priority 4: Quality

#### RAG recall quality investigation
- search_memories returned relevant results for 5/6 queries, but some were noisy
- Classification coverage is low: 164 classifications vs 6,164 episodes (2.6%)
- Consider: cycle1 backfill to increase classification coverage
- Consider: tuning weighted_score thresholds for search results

#### Skill trigger verification
- Next session: verify all 3 skills (workflow, search, recall) actually auto-trigger
- Test scenarios: research request, session start recall, unfamiliar term lookup
- If still not triggering, may need hook-based enforcement instead of description-based

### Priority 5: Deferred

#### cycle3 implementation
- Weekly decay application + context.md rebuild as separate cycle
- Currently decay is computed at search time only, context.md refresh is part of cycle2

#### Memory security scanning
- Prompt injection detection on memory writes
- Credential leak detection
- Invisible unicode character blocking

#### Gateway abstraction (trib-channels)
- Abstract Discord backend for multi-platform support
- Current: Discord-only with backend interface defined but single implementation
