#!/usr/bin/env node
/**
 * Orchestrator CLI
 *
 * Usage:
 *   node cli.js ask <sessionId> <prompt>                    — foreground
 *   node cli.js ask --background <sessionId> <prompt>       — background (detach)
 *   node cli.js ask <sessionId> --context "extra" <prompt>
 */
export {};
