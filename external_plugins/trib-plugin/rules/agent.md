# Agent

- Standalone tasks: Agent + run_in_background: true
- GPT calls: use ask-forwarder agent, not Bash
- Team workflow: TeamCreate → TaskCreate → Agent assignment
- Agent names: Agent-a, Agent-b, Agent-c, Agent-d
