# Agent

- Standalone tasks: Agent + run_in_background: true
- Quick questions: use /ask (ask-forwarder)
- Team tasks: use /assign with preset name
- Team workflow: TeamCreate → TaskCreate → /assign per task
- Agent names: Agent-a, Agent-b, Agent-c, Agent-d
- Native preset (worker) → Worker agent
- External preset (bridge) → Bridge agent
