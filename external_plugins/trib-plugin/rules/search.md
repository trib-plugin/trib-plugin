# Search

- All external information lookups MUST use the `mcp__plugin_trib-plugin_trib-plugin__search` tool (MCP). Built-in WebSearch/WebFetch are forbidden.
- Use `mcp__plugin_trib-plugin_trib-plugin__batch` for 2+ concurrent lookups.
- When unsure, search first. Never guess.
