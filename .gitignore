node_modules/
*.sqlite
*.sqlite-shm
*.sqlite-wal
.DS_Store
.venv/

# Plugin runtime data (never commit — per-user local state)
external_plugins/*/.data/
external_plugins/*/mcp.lock
external_plugins/*/mcp-debug.log
external_plugins/*/tools-cache.json
external_plugins/*/.embeddings.cache.json

# Local dev / bench scripts and their outputs (not for distribution)
external_plugins/*/dev/
